# Server Configuration
PORT=8080
DATABASE_URL=postgres://user:password@localhost:5432/council?sslmode=disable
TAVILY_API_KEY=tvly-xxxxxxxxxxxx

# ==============================================================================
# Global Provider Keys (Credentials Vault)
# ==============================================================================
# Set these keys once to be used by both LLM and Embedding services unless overridden.
OPENAI_API_KEY=sk-xxxxxxxxxxxx
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxx
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxx
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxx
GEMINI_API_KEY=AIzaSyxxxxxxxxxx

# ==============================================================================
# System Default Chat Model (LLM)
# ==============================================================================
# Used for system-level tasks (Wizard Mode, Fallback). 
# Individual Agents usually have their own config in the DB.
LLM_PROVIDER=openai             # Options: openai, deepseek, siliconflow, dashscope, gemini, ollama
LLM_MODEL=gpt-4o                # Default model
# LLM_API_KEY=                  # Optional: Override global key if needed
# LLM_BASE_URL=                 # Optional: Only needed for Ollama or custom proxies

# ==============================================================================
# System Wide Embedding Configuration
# ==============================================================================
# CRITICAL: Must be consistent across the system for vector compatibility.
# The schema expects 1536-dimensional vectors.
EMBEDDING_PROVIDER=siliconflow  # Default: siliconflow (Recommended for cost/performance)
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B # Default 1536-dim model for SiliconFlow
# EMBEDDING_API_KEY=            # Optional: Override global key
# EMBEDDING_BASE_URL=           # Optional: Needed for Ollama

# --- Recommended Embedding Combos for 1536 Dimensions ---
# SiliconFlow: Provider=siliconflow, Model=Qwen/Qwen3-Embedding-8B
# OpenAI:      Provider=openai,      Model=text-embedding-3-small
# DashScope:   Provider=dashscope,   Model=text-embedding-v1
# Ollama:      Provider=ollama,      Model=gte-qwen2-1.5b-instruct-embed-f16 (Manual pull required)
